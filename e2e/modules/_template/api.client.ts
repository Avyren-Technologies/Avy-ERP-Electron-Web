import { BaseApiClient } from '../../shared/helpers/base-api-client';

/**
 * TODO: Rename this class for your module.
 * Example: HRMSApiClient, ProductionApiClient, InventoryApiClient
 */
export class TemplateApiClient extends BaseApiClient {
  // Add your module's API methods here.
  // Use the inherited helpers: this.get(), this.post(), this.patch(), this.delete()
  //
  // Example:
  //   async listEmployees(params?: Record<string, string>) {
  //     return this.get('/hr/employees', params);
  //   }
  //
  //   async createEmployee(data: Record<string, unknown>) {
  //     return this.post('/hr/employees', data);
  //   }
}
